{
  "name": "ajax-cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npm run verify",
    "verify": "cargo fmt --check && cargo check --all-targets --all-features && cargo clippy --all-targets --all-features -- -D warnings && cargo nextest run --all-features --test-threads=1 && cargo test --doc && npm run lint:duplication",
    "lint:duplication": "jscpd --config .jscpd.json",
    "verify:probity": "node scripts/verify-probity.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "@nizos/probity": "^1.5.0",
    "husky": "^9.1.7",
    "jscpd": "^4.2.3"
  }
}
