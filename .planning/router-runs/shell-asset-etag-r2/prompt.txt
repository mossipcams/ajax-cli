You are a bounded implementation worker for a parent agent.
Current directory is the task worktree. Your previous round is already applied.
Never commit, push, merge, rebase, create branches, or change branches.

REVIEW GATE FINDING (one bounded fix):

The new test `static_shell_assets_revalidate_with_a_weak_etag` in
crates/ajax-web/src/runtime.rs never exercises the actual production request
shape. Real iOS Safari always sends `accept-encoding: gzip` alongside
`if-none-match`. CompressionLayer wraps the router and runs AFTER the handler,
so the gzip + conditional-GET combination is untested and could regress the
whole feature.

REQUIRED CHANGE:
In that same test, for `/app.js` only, add one more request that sends BOTH
  .header("accept-encoding", "gzip")
  .header("if-none-match", &etag)
and assert it returns StatusCode::NOT_MODIFIED with an empty body and
`etag == etag`. Also assert the 200 path with `accept-encoding: gzip` still
carries the same weak `etag` header (compression must not rewrite or drop the
validator).

If the assertion fails, that is a real defect: report it as BLOCKED with the
observed headers rather than weakening the assertion or changing production
code to make it pass.

IMMUTABLE CONSTRAINTS (unchanged from the packet):
- Allowed files: crates/ajax-web/src/adapters/http.rs, crates/ajax-web/src/runtime.rs
- Do not add immutable, max-age, or ?v= cache busting.
- Do not change the shell or /api/* away from no-store.
- No renames, formatting sweeps, or drive-by cleanup.
- Smallest possible edit; this should be well under 40 changed lines.

Verification:
  cargo test -p ajax-web static_shell_assets_revalidate_with_a_weak_etag
  cargo nextest run -p ajax-web
  cargo clippy -p ajax-web --all-targets --all-features -- -D warnings
  cargo fmt --check

Return exactly the DELEGATE_REPORT schema between these markers:
ROUTER_REPORT_BEGIN
<DELEGATE_REPORT YAML with STATUS, SUMMARY, FILES_CHANGED, TEST_FIRST,
COMMAND_EVIDENCE (PHASE/COMMAND/EXIT_CODE/OUTPUT_EXCERPT),
STOP_CONDITIONS_HIT, REMAINING_RISKS>
ROUTER_REPORT_END
Include every command's actual exit code and a short output excerpt.
