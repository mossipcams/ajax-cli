# PR 1184 CI repair

Scope: inspect both failed jobs at head 352ca3f; fix only branch-caused failures in crates. No tests/ changes, unrelated repairs, or lifecycle/security changes.
Approval: user authorized investigation, fixes, local verification, commit and push.

- [x] Inspect CI job 105299714449 and Rust Lint job 105298964289 from run 35249744632; establish causality.
- [x] Apply minimal relevant repair.
- [x] Run focused verification and repository local gate.
- [x] Commit and push existing PR branch; parent review delta.

## Failure evidence (head 352ca3f)

### Rust Lint (job 105298964289)
- Step: `cargo fmt --check`
- File: `crates/ajax-web/src/slices/web_session/task_session_idle_eviction_tests.rs:430`
- Diff: `assert_eq!` args `child_before, child_after` must be on one line per rustfmt
- Exit code 1

### CI (job 105299714449)
- Step: `Verify required checks`
- Message: `Required rust lane job did not pass (result: failure).`
- Causality: cascade from Rust Lint failure (rust lane first job = failure)

## Repair
- `cargo fmt` on `task_session_idle_eviction_tests.rs` (inline test module in `src/`, not `tests/` dir)

## Verification
- `cargo fmt --check` — pass (exit 0)
- `cargo test -p ajax-web task_session_idle_eviction` — 8 passed

## Local verification gate (2026-09-17)

Husky configured:
- `.husky/pre-commit` runs `node scripts/check-file-loc.mjs --staged` and `cargo fmt --check`
- `.husky/commit-msg` strips Cursor attribution via `scripts/strip-cursor-attribution`
- `package.json` has `"prepare": "husky"` and `husky@^9.1.7`
- After `npm ci`, `git config core.hooksPath` → `.husky/_`; hook stubs present

Gate commands (documented PR gate; file LOC uses branch diff vs `origin/main`):

```text
FILE_LOC_BASE=$(git rev-parse origin/main) FILE_LOC_HEAD=$(git rev-parse HEAD) node scripts/check-file-loc.mjs
# exit 0 — Checked 11 changed source file(s) between 8c5f9e9 and dedfe4d.
# 3 warning(s), 0 error(s) (client.rs 967, client_tests.rs 857, sdk_connection.rs 959 lines; all under FAIL_AT 1000)

cargo fmt --check
# exit 0
```

Validation: complete. Deviations: none. Parent review: accepted; diff from supplied PR head changes only assertion whitespace and this plan. The snapshot also includes pre-existing remote changes brought in by fast-forward to the supplied head.
