[project]
name = "ajax-textual"
version = "0.1.0"
description = "Textual cockpit frontend for Ajax CLI"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.85",
]

[project.scripts]
ajax-textual = "ajax_textual:main"
